# SendGrid Email Configuration
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Email address to send FROM (must be verified in SendGrid)
# Go to: https://app.sendgrid.com/settings/sender_auth/senders
EMAIL_FROM=noreply@yourdomain.com

# MongoDB Connection String
MONGODB_URL=mongodb://localhost:27017/LargeProject

# ============================================
# SETUP INSTRUCTIONS:
# ============================================
# 1. Copy this file to .env (without .example)
# 2. Sign up for SendGrid: https://signup.sendgrid.com/
# 3. Create an API key with "Mail Send" permissions
# 4. Add and verify a sender email address
# 5. Replace the values above with your actual credentials
# 6. Make sure .env is in your .gitignore file
